# 开发规划 · 美术视觉增强

> 状态：**待美术人员到位后启动**
> 背景：翻牌小游戏、工厂可视化、图鉴等核心玩法已上线，当前全部使用 emoji 占位。有美术资源可投入后，按本规划分阶段升级视觉。

---

## 整体原则

- 风格建议：**工业霓虹扁平风**（深色底 + 青色/金色线条），与现有界面（#00bcd4 青 / #ffb000 金 / 深色底）统一
- 图片资源放 `public/img/`，前端 `<img>` / CSS 背景引用，**单 HTML 零构建完全兼容**
- 卡牌图标走 `data/term-cards.json` 加 `icon` 字段（数据驱动，改 JSON 即生效，不碰 JS）
- 灰色/锁定态优先用 CSS `filter: grayscale` 处理，减少一半美术量

---

## P0 · 卡牌系统美术化（最高优先级，直接提升翻牌体验）

### 1. 词条图标（29 张，128×128 PNG 透明底）
| 分类 | 词条 |
|---|---|
| 装机 | 虚拟机、操作系统、Shell、apt |
| 网络 | 网卡、IP 地址、MAC、子网掩码、CIDR、回环地址、路由表、IPv4、IPv6、NAT、网关、端口、路由、域名、DNS、反向解析、nslookup、tracert、ping |
| 安全/协议 | 防火墙、iptables、DDoS、TCP、UDP、OSI |

### 2. 卡背图案（1 张，统一主题：齿轮 + 数据流 + 锐智工厂 LOGO）
替换翻牌背面的 "❔"，用于所有翻牌关卡。

### 3. 稀有度系统（游戏化收集钩子，强烈推荐）
词条分级，卡面用不同边框/质感，激励学生反复刷翻牌收集：
- 🌟 **史诗（金框）**：ping、DNS、网关、防火墙、IP 地址、虚拟机、Shell
- 💎 **稀有（蓝框）**：网卡、MAC、子网掩码、端口、tracert、路由、域名、操作系统、apt
- ⬜ **普通（绿框）**：其余 13 个
- 图鉴按稀有度展示，稀有卡点亮更有成就感

---

## P1 · 工厂场景手绘（让"点亮工厂"有视觉冲击）

- 8 张横图（800×400）：中控室、操作台、车间、边缘柜、云平台、大屏、数据管道、AI 实验室
- 每区域两态：**灰暗锁定**（CSS grayscale）vs **彩色点亮**
- 关卡完成 → 区域点亮动画；全厂通关 → 整体庆祝

---

## P2 · 厂长立绘（增强叙事代入）

- Q 版厂长立绘，5 个情绪版本：普通、得意😎、严肃😤、思考🤔、尴尬😅
- 接现有情绪系统（directorMood），对话时切换表情

---

## 排期建议（按美术工作量）

1. **P0 第一批**：卡背 1 + 核心图标 10（先覆盖块①装机 / 块④寻路 / 块⑤安全）+ 稀有度边框 3 种 ≈ 14 张 → 一周见效
2. **P0 第二批**：剩余图标 19 张
3. **P1 工厂**：8 张（灰色版 CSS 滤镜）
4. **P2 厂长立绘**：5 张

---

## 技术接入备忘（人员到位后）

- 卡牌图标：`term-cards.json` 每对加 `icon: '/img/cards/xxx.png'`，前端渲染 `<img>` 替代 emoji
- 稀有度：`term-cards.json` 每对加 `rarity: 'epic'|'rare'|'common'`，CSS 三种边框类
- 工厂：`renderFactory` 里区域图 `<img>` + CSS filter 切灰/点亮
- 厂长：`director-avatar` 换立绘，按情绪切换 src
