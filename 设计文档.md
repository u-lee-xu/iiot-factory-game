# 闯关游戏 — 设计文档

## 技术栈

| 层 | 技术 | 选型理由 |
|----|------|---------|
| 后端 | Node.js + Express | 零配置，单文件可起服务 |
| 数据库 | SQLite (better-sqlite3) | 无需安装数据库，单文件存储，并发安全 |
| 前端 | 原生 HTML/CSS/JS | 无构建步骤，打开即用 |
| 部署 | 单进程，端口 3000 | 局域网 IP 访问，后续可加 nginx 反代上公网 |

---

## 一、后端设计

### 1.1 数据库设计

```sql
-- 学生表
CREATE TABLE students (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT    UNIQUE NOT NULL,        -- 学生姓名
  password    TEXT    DEFAULT '',              -- 预留密码字段，公网部署时启用
  check_data  TEXT    DEFAULT '{}',            -- 任务完成状态 JSON
  stars_data  TEXT    DEFAULT '{}',            -- 星级评价数据 JSON
  created_at  TEXT    DEFAULT (datetime('now'))
);

-- 教师表（只有一条记录）
CREATE TABLE teacher (
  id       INTEGER PRIMARY KEY CHECK (id = 1),
  password TEXT NOT NULL DEFAULT 'admin123'
);

-- 会话表（简单 token 鉴权）
CREATE TABLE sessions (
  token      TEXT PRIMARY KEY,
  role       TEXT NOT NULL,          -- 'teacher' | 'student'
  name       TEXT NOT NULL,
  expires_at TEXT NOT NULL
);
```

### 1.2 API 设计

#### 公开接口

| 方法 | 路径 | 入参 | 出参 | 说明 |
|------|------|------|------|------|
| POST | /api/login | `{ name, password? }` | `{ ok, role, token, data? }` | 教师/学生登录 |

#### 学生接口（需 token）

| 方法 | 路径 | 入参 | 出参 | 说明 |
|------|------|------|------|------|
| GET | /api/student/me | header: token | `{ name, check, stars, xp }` | 获取自己的进度 |
| PUT | /api/student/me | header: token + `{ check?, stars? }` | `{ ok, xp }` | 更新自己的进度 |

#### 教师接口（需 token + role=teacher）

| 方法 | 路径 | 入参 | 出参 | 说明 |
|------|------|------|------|------|
| GET | /api/teacher/students | header: token | `[{ name, check, stars, xp }]` | 所有学生列表 |
| POST | /api/teacher/students | `{ names: ["A","B"] }` | `{ ok, count }` | 批量添加学生 |
| DELETE | /api/teacher/student/:name | header: token | `{ ok }` | 删除学生 |
| PUT | /api/teacher/student/:name/grade | `{ levelId: 1, stars: { teacher: 4 } }` | `{ ok }` | 教师打分 |
| POST | /api/teacher/student/:name/reset | — | `{ ok }` | 重置该学生进度 |

#### 错误码

| code | 含义 |
|------|------|
| 401 | 未登录或 token 过期 |
| 403 | 无权限（非教师访问教师接口） |
| 404 | 学生不存在 |
| 409 | 学生名已存在 |

### 1.3 鉴权方案

```
学生登录: POST /api/login { name } 
  → 验证 name 存在于 students 表
  → 生成随机 token，写入 sessions 表（有效期24h）
  → 返回 { ok:true, role:"student", token, data:{...} }

教师登录: POST /api/login { name:"teacher", password:"xxx" }
  → 验证 password 匹配 teacher 表
  → 生成随机 token
  → 返回 { ok:true, role:"teacher", token }

后续请求: header: Authorization: Bearer <token>
```

---

## 二、前端设计

### 2.1 页面结构

```
/login.html          — 登录页（学生选身份 / 教师入口）
/student.html        — 学生主界面（关卡地图 + 任务 + 星级评价）
/teacher.html        — 教师管理界面（学生列表 + 进度 + 打分）
```

### 2.2 路由设计

纯静态页面，由 HTML 文件切换，不涉及前端路由。
- 登录成功后，前端根据 role 跳转 `location.href = 'student.html'` 或 `teacher.html`
- 各页面在 onload 时请求对应 API 获取数据

### 2.3 状态管理

```
全局状态（存于 sessionStorage，刷新不丢失，关标签页清除）：
  - token: string          — API 鉴权用
  - role: 'student'|'teacher'
  - name: string

学生页面局部状态：
  - levels: Level[]        — 关卡数据（前端硬编码，同旧版 HTML）
  - check: { [key]: bool } — 任务勾选状态（每次修改后 PUT 到后端）
  - stars: { [levelId]: { self, peer, teacher } }

教师页面局部状态：
  - students: Student[]    — 所有学生数据（GET /api/teacher/students）
  - selectedStudent: string
```

### 2.4 UI 交互流程

#### 登录页
```
┌─────────────────────────────────────┐
│  锐智工厂 · 工业互联网闯关          │
│                                     │
│  姓名 [________________]            │
│  密码 [________________] (可选)     │
│                                     │
│  [进入闯关]  [我是教师]             │
│                                     │
│  提示：请使用老师分配的姓名登录      │
└─────────────────────────────────────┘
```

**交互状态：**
- 空态：展示标题和输入框
- 加载：按钮置灰，"登录中…"
- 错误：红色提示文字（"该姓名不存在，请联系老师添加"）
- 成功：跳转 student.html / teacher.html

#### 学生页（改造旧版 HTML）
```
┌─────────────────────────────────────────────┐
│  ⚙ 锐智工厂                  张三 | 学徒   │
│  [████████████░░░░░░░]  XP: 3200/4500       │
├─────────────────────────────────────────────┤
│  L1─L2─L3─L4─L5─L6─L7─L8   (关卡地图)      │
├─────────────────────────────────────────────┤
│  L3 · 设备联网攻坚战                        │
│  ☑ 环境准备...      100XP                  │
│  ☑ 任务1-1...       100XP    ← 点击勾选    │
│  ☐ 任务1-2...       100XP                   │
│  ...                                        │
│                                              │
│  ⭐ 星级评价                                │
│  自评 ★★★★☆   互评 ★★★☆☆   师评 ★★★★☆   │
│  综合：★★★★☆                              │
├─────────────────────────────────────────────┤
│  🏅 🖥️ 🔐 🔧 ⚡ 📦 📊 🔗                  │
└─────────────────────────────────────────────┘
```

- 每次勾选任务 → PUT /api/student/me → 刷新 XP 显示
- 每次修改星级 → PUT /api/student/me → 刷新综合星级
- 底部成就徽章：根据任务完成度自动更新

#### 教师页
```
┌─────────────────────────────────────────────┐
│  ⚙ 锐智工厂 · 教师端            [退出]      │
├─────────────────────────────────────────────┤
│  学生管理                                    │
│  [批量添加] [输入姓名用逗号分隔]             │
│                                              │
│  ┌──────┬──────┬──────┬──────┬──────┐       │
│  │ 姓名 │ 等级 │ XP   │ 完成度│ 操作  │       │
│  ├──────┼──────┼──────┼──────┼──────┤       │
│  │ 张三 │ 技工 │ 3200 │ 68%  │ [打分]│       │
│  │ 李四 │ 学徒 │ 1800 │ 42%  │ [打分]│       │
│  │ 王五 │ 实习 │  800 │ 15%  │ [打分]│       │
│  └──────┴──────┴──────┴──────┴──────┘       │
│                                              │
│  点击[打分]展开该生详情 + 师评打分界面       │
└─────────────────────────────────────────────┘
```

### 2.5 组件划分

| 组件 | 职责 | 出现在 |
|------|------|--------|
| LevelMap | 渲染 8 个关卡节点 + 连线 | student.html |
| TaskList | 渲染任务清单，响应点击勾选 | student.html |
| StarRater | 星级选择器（自评/互评/师评） | student.html, teacher.html |
| AchievementGrid | 展示成就徽章 | student.html |
| StudentTable | 学生列表表格 | teacher.html |
| GradePanel | 展开详情 + 师评打分 | teacher.html |

---

## 三、模块依赖

```
server.js (入口)
  ├── db.js            — 数据库初始化 + 查询方法
  ├── auth.js          — 鉴权中间件 + token 管理
  ├── routes/
  │   ├── login.js     — POST /api/login
  │   ├── student.js   — GET/PUT /api/student/me
  │   └── teacher.js   — 教师管理接口
  └── public/          — 静态文件
      ├── login.html
      ├── student.html
      └── teacher.html

数据流:
  login.html → POST /api/login → token → student.html/teacher.html
  student.html → GET /api/student/me → 渲染关卡
  student.html → PUT /api/student/me → 保存进度
  teacher.html → GET /api/teacher/students → 渲染列表
  teacher.html → PUT /.../grade → 保存师评
```

---

## 四、设计决策记录

| # | 决策 | 方案 | 替代方案 | 理由 |
|---|------|------|---------|------|
| 1 | 数据库 | SQLite | JSON 文件 | 并发安全，支持多人同时写入 |
| 2 | 鉴权 | Bearer Token | Cookie Session | 前端纯静态，无 Cookie 管理依赖 |
| 3 | 前端框架 | 原生 JS | React/Vue | 零构建，单 html 文件无依赖 |
| 4 | 教师密码 | 硬编码默认值 | 环境变量 | MVP 阶段简化，后续改为环境变量 |
| 5 | Token 有效期 | 24 小时 | 永久/7天 | 课堂教学当天有效，安全 + 实用 |
